import { ScrollView, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function MilestoneDetail({navigation, route}:any){
    return <SafeAreaView>
        <ScrollView>
            <Text>2</Text>
        </ScrollView>
    </SafeAreaView>
}
